# Core dependencies
elevenlabs>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Audio processing (lightweight — librosa pulled in only if needed locally)
pydub>=0.25.1
soundfile>=0.12.1
numpy>=1.24.0
mutagen>=1.47.0

# Data handling
pandas>=2.0.0
openpyxl>=3.1.0

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
tqdm>=4.65.0
click>=8.1.0
